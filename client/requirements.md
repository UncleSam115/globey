## Packages
framer-motion | Complex animations and transitions
react-simple-maps | Interactive SVG world map
d3-scale | Color scales for the map
date-fns | Date formatting
clsx | Class name conditional logic
tailwind-merge | Merging tailwind classes

## Notes
- Map component will fetch TopoJSON from a public CDN
- Auth is handled via Replit Auth (/api/login, /api/logout)
- Stripe checkout redirects to external URL returned by API
